{
  "name": "marian-ai",
  "version": "1.0.0",
  "private": true,
  "description": "MARIAN.AI — Fullstack Modular Workspace",
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "cd backend && venv/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload",
    "test:backend": "cd backend && PYTHONPATH=. venv/bin/pytest",
    "lint:backend": "cd backend && venv/bin/ruff check ."
  }
}
